#include <stdio.h>

/**
 * main - program that prints the number of arguments passed into i
 * @argc:  indicates how many arguments were entered on the command line
 * @argv: contains the arguments passed to a program
 * Return: 0
 */

void main(int argc, char const *argv[])
{
	argv =argv;
	printf("%i\n", argc - 1);
	return (0);
}
