#include "main.h"

/**
 * print_most_numbers - print numbers from 0 to 9 except 2 and 4
 *
 * Return: void
 */
void print_most_numbers(void)
{
	int n;

	for (n = 48; n < 58; n++)
	{
		if (( == 50) || ( == 52))
		{
			continue;
		}
		_putchar(10);
	}
	_putchar(10);
}
